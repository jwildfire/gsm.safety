% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Input_Participants.R
\name{ParticipantCounts}
\alias{ParticipantCounts}
\title{Which rows of a domain count towards a census figure?}
\usage{
ParticipantCounts(dfDomain, strFilterCol = NULL, strFilterValues = NULL)
}
\arguments{
\item{dfDomain}{\code{data.frame} Domain being counted.}

\item{strFilterCol}{\code{character} or \code{NULL} Deciding column.}

\item{strFilterValues}{\code{character} or \code{NULL} Values that count, comma
separated, compared without regard to case or surrounding space.}
}
\value{
\code{logical} of \code{nrow(dfDomain)}.
}
\description{
Three cases, and the difference between them is the whole configurability of
the counting metrics: no filter column counts every row; a filter column
with no values counts a row that \emph{records} something; a filter column with
values counts a row that matches one of them.
}
\details{
Missing is never a match. A row carried for some other reason, or one whose
completion flag was never filled in, must not read as a completion.
}
\keyword{internal}
