% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Widget_HepExplorer.R
\name{Widget_HepExplorer}
\alias{Widget_HepExplorer}
\title{Hepatic Safety Explorer (eDISH) Widget}
\usage{
Widget_HepExplorer(
  dfResults,
  lSettings = list(),
  width = NULL,
  height = NULL,
  elementId = NULL,
  bDebug = FALSE
)
}
\arguments{
\item{dfResults}{\code{data.frame} Long-format results data, one record per
measurement. Column names are supplied by \code{lSettings}; the defaults
expect \code{TEST}/\code{STRESN}/\code{USUBJID}/\code{STNRHI} (see
\code{inst/schema/hep-explorer.json}). The \code{measure_values} setting maps the
ALT/AST/TB/ALP keys onto the dataset's measure names.}

\item{lSettings}{\code{list} safety.viz hep-explorer settings overrides; merged
onto the module's \code{DEFAULT_SETTINGS} client-side, so only overrides are
needed.}

\item{width}{\code{character} Width of the widget as a CSS unit. Default: \code{NULL}.}

\item{height}{\code{character} Height of the widget as a CSS unit. Default:
\code{NULL}.}

\item{elementId}{\code{character} ID of the widget's HTML element. Default:
\code{NULL}.}

\item{bDebug}{\code{logical} Print debug messages in the browser console?
Default: \code{FALSE}.}
}
\description{
A widget that renders the safety.viz hepatic safety explorer: an eDISH
scatter plot of peak liver measures (ALT vs. total bilirubin, as multiples
of the upper limit of normal) with Hy's Law quadrants, participant
drill-down to a standardized-values-by-study-day view, and a linked
measure table.
}
\examples{
dfResults <- ExampleData("adbds")

Widget_HepExplorer(
  dfResults,
  lSettings = list(
    studyday_col = "VISITNUM",
    visit_col = "VISIT",
    visitn_col = "VISITNUM",
    measure_values = list(
      ALT = "Alanine Aminotransferase",
      AST = "Aspartate Aminotransferase",
      TB = "Bilirubin",
      ALP = "Alkaline Phosphatase"
    ),
    filters = list(
      list(value_col = "SEX", label = "Sex"),
      list(value_col = "ARM", label = "Treatment Group")
    ),
    groups = list(
      list(value_col = "ARM", label = "Treatment Group"),
      list(value_col = "SEX", label = "Sex")
    )
  )
)

}
