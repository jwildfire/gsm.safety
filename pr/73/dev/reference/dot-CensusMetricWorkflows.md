# Every census metric definition the package ships

Every census metric definition the package ships

## Usage

``` r
.CensusMetricWorkflows()
```

## Value

Named `list` of workflows, one per ID in
[.CENSUS_METRIC_IDS](https://jwildfire.github.io/gsm.safety/dev/reference/dot-CENSUS_METRIC_IDS.md).
