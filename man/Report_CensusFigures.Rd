% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Report_CensusFigures.R
\name{Report_CensusFigures}
\alias{Report_CensusFigures}
\title{The census figures, as the report presents them}
\usage{
Report_CensusFigures(dfResults, dfMetrics, lSettings)
}
\arguments{
\item{dfResults}{\code{data.frame} The reporting model's results —
\code{Reporting_Results}, one row per metric per group, carrying \code{MetricID},
\code{Numerator} and \code{Denominator}.}

\item{dfMetrics}{\code{data.frame} The reporting model's metric definitions —
\code{Reporting_Metrics}, carrying \code{ID}, \code{MetricID}, \code{Metric} and the
\code{Numerator} and \code{Denominator} labels.}

\item{lSettings}{\code{list} The report workflow's settings: \code{Sections}, each a
\code{Title} and the \code{Metrics} read out under it, and \code{PersonTime}, naming the
metric IDs published in days along with \code{DaysPerYear} and the \code{Unit} to
present them in.}
}
\value{
\code{data.frame}, one row per published figure, with columns \code{Section},
\code{ID}, \code{Figure}, \code{Value}, \code{Unit}, \code{Denominator} and \code{DenominatorLabel}.
}
\description{
Reads the rows the census metrics published and lays them out in the order
and the sections the report workflow declares. It counts nothing: every
value on the page is the \code{Numerator} a metric published, beside the
\code{Denominator} that same metric published, under the labels that metric's own
definition carries. A figure the metrics did not publish is not here — see
\code{\link[=Report_CensusProvenance]{Report_CensusProvenance()}}, which names it as absent rather than leaving a
blank row on the page.
}
\section{Days published, years presented}{


The person-time metrics publish participant-days, because days are the
number actually summed and a metric publishing years would publish a figure
nothing in the pipeline can check against the domain it came from. The
report divides, and only for the metric IDs the workflow names under
\code{PersonTime}. The published days stay on the page in the provenance table,
so the division is checkable rather than taken on trust.
}

\section{Nothing flags}{


D0023.3 — the census metrics declare no threshold and publish an empty flag,
and there is no flag column here. A result that arrives carrying a flag is
refused rather than presented; see \code{\link[=RefuseFlaggedResults]{RefuseFlaggedResults()}}.
}

\examples{
dfResults <- data.frame(
  MetricID = "Analysis_saf0004", GroupID = "AA-AA-000-0000",
  GroupLevel = "Study", Numerator = 13, Denominator = 762,
  Metric = 13 / 762, Score = 13, Flag = NA_integer_
)
dfMetrics <- data.frame(
  ID = "saf0004", MetricID = "Analysis_saf0004",
  Metric = "Deaths (Study)", Numerator = "Participants Who Died",
  Denominator = "Enrolled Participant"
)
Report_CensusFigures(
  dfResults, dfMetrics,
  lSettings = list(
    Sections = list(list(Title = "Population", Metrics = list("saf0004")))
  )
)

}
