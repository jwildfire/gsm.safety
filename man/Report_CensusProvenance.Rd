% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Report_CensusProvenance.R
\name{Report_CensusProvenance}
\alias{Report_CensusProvenance}
\title{What each census metric published, verbatim}
\usage{
Report_CensusProvenance(dfResults, dfMetrics, lSettings)
}
\arguments{
\item{dfResults}{\code{data.frame} The reporting model's results —
\code{Reporting_Results}, one row per metric per group, carrying \code{MetricID},
\code{Numerator} and \code{Denominator}.}

\item{dfMetrics}{\code{data.frame} The reporting model's metric definitions —
\code{Reporting_Metrics}, carrying \code{ID}, \code{MetricID}, \code{Metric} and the
\code{Numerator} and \code{Denominator} labels.}

\item{lSettings}{\code{list} The report workflow's settings: \code{Sections}, each a
\code{Title} and the \code{Metrics} read out under it, and \code{PersonTime}, naming the
metric IDs published in days along with \code{DaysPerYear} and the \code{Unit} to
present them in.}
}
\value{
\code{data.frame}, one row per metric named in \code{lSettings}, with columns
\code{ID}, \code{Figure}, \code{Numerator}, \code{Denominator} and \code{Status}.
}
\description{
Every metric the report was configured to read, in the order it reads them,
with the numbers exactly as published — participant-days in days, counts as
counts — and, where a metric published nothing, a line saying so.
}
\details{
This table is what makes the report auditable. The figures table presents;
this one records. A reader who wants to check that the exposure figure was
divided rather than recounted can read the days here and the years there,
and a metric that stopped because a study supplies no such domain is named
rather than silently missing.
}
\section{Three states, kept apart}{


\code{Status} distinguishes the three states the census metrics are built to keep
apart. \strong{published} is a figure, and a published \code{0} is a measurement.
\strong{no row published} is a metric that ran and measured nothing — the domain
was absent or empty, and it stopped rather than reporting a zero.
\strong{not run for this study} is a metric the report was told to read that this
study's reporting model does not carry at all: the study never ran it. On
any study with no ECG domain that is \code{saf0011}, because a batch that
includes it stops on the missing domain rather than publishing a zero.
}

\examples{
Report_CensusProvenance(
  dfResults = data.frame(
    MetricID = "Analysis_saf0008", GroupID = "AA-AA-000-0000",
    Numerator = 26754, Denominator = 762, Flag = NA_integer_
  ),
  dfMetrics = data.frame(
    ID = c("saf0008", "saf0011"),
    MetricID = c("Analysis_saf0008", "Analysis_saf0011"),
    Metric = c(
      "Participant-Days on Study (Study)", "Participants With an ECG (Study)"
    )
  ),
  lSettings = list(
    Sections = list(
      list(Title = "Exposure", Metrics = list("saf0008", "saf0011"))
    )
  )
)

}
