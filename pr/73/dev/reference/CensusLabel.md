# A label from a metric definition, or an empty string

A label from a metric definition, or an empty string

## Usage

``` r
CensusLabel(dfDef, strCol)
```

## Arguments

- dfDef:

  `data.frame` One definition row.

- strCol:

  `character` Column to read.

## Value

`character(1)`.
