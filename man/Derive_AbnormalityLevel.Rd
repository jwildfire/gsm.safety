% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Derive_AbnormalityLevel.R
\name{Derive_AbnormalityLevel}
\alias{Derive_AbnormalityLevel}
\title{Grade a result against the FDA abnormality level criteria}
\usage{
Derive_AbnormalityLevel(
  dfResults,
  dfCriteria = gsm.safety::FDA_AbnormalityLevels,
  strTestCol = "TEST",
  strValueCol = "STRESN",
  strULNCol = "STNRHI",
  strUnitCol = "STRESU",
  strSexCol = NULL,
  lParameterValues = DefaultAbnormalityParameters(),
  strOutCol = "AbnormalityLevel"
)
}
\arguments{
\item{dfResults}{\code{data.frame} Long-format results, one row per participant
per test per visit, in the shape of \code{ExampleData("adbds")}.}

\item{dfCriteria}{\code{data.frame} The criteria, in the shape of
\link{FDA_AbnormalityLevels} (at least \code{Parameter}, \code{Direction}, \code{Qualifier},
\code{Unit}, \code{Basis}, \code{Operator}, \code{Level1}, \code{Level2}, \code{Level3}). Default:
\code{FDA_AbnormalityLevels}.}

\item{strTestCol}{\code{character} Column holding the test name. Default: \code{"TEST"}.}

\item{strValueCol}{\code{character} Numeric result column. Default: \code{"STRESN"}.}

\item{strULNCol}{\code{character} Upper-limit-of-normal column. Default: \code{"STNRHI"}.}

\item{strUnitCol}{\code{character} Unit column. Default: \code{"STRESU"}.}

\item{strSexCol}{\code{character} Sex column (\code{M}/\code{F} or \code{Male}/\code{Female}, any
case), used for the sex-qualified rows. Default: \code{NULL}, which skips them.}

\item{lParameterValues}{Named \code{list} mapping each criteria \code{Parameter} to
the test name(s) the data use for it. Default: the names
\code{ExampleData("adbds")} uses.}

\item{strOutCol}{\code{character} Name of the grade column. Default:
\code{"AbnormalityLevel"}; the direction and the criterion met are added as
\verb{<strOutCol>} with \code{Level} replaced by \code{Direction} and \code{Criterion}.}
}
\value{
\code{dfResults} with three columns appended: the grade (\code{integer}
0 to 3, or \code{NA}), the direction of the row that graded it (\code{"low"},
\code{"high"}, \code{"increase"}, \code{"decrease"}, or \code{NA}), and the criterion text as
printed in the guide (for example \code{">5.5"}), \code{NA} where no level was met.
}
\description{
Adds the level 1, 2 or 3 abnormality grade from Appendix Tables 56
(chemistry) and 57 (hematology) of the FDA Standard Safety Tables and
Figures Integrated Guide v2.0, the criteria the guide's "analyte values
exceeding specified levels" tables count on (section 2.4.3). The levels are
cumulative, so the grade is the highest level the result meets in the
row's direction with the row's operator, \code{0} when the parameter is graded
and the result meets none, and \code{NA} when the record cannot be graded.
Requirement \code{FDA-RULE-002}; the sex-qualified rows are \code{FDA-RULE-016}.
}
\details{
What is graded, and what is left \code{NA}:
\itemize{
\item \code{uln_multiple} rows (the liver panel, CPK, amylase, lipase, PT, PTT) are
graded on the result divided by its own ULN, as \code{\link[=Derive_ULNMultiple]{Derive_ULNMultiple()}}
forms it, whatever unit the data carry.
\item \code{absolute} rows are graded only when the record's unit matches the
criteria's unit. The guide prints US-conventional units; a result in
another unit is left \code{NA} and named in a message rather than compared on
the wrong scale. Spellings of the same unit are folded (\code{mEq/L} and
\code{mmol/L} for the monovalent electrolytes, \code{GI/L} and \verb{x 10^9 cells/L}),
but nothing is converted.
\item Rows with a sex qualifier (HDL; hemoglobin, low) are applied only when
\code{strSexCol} names a column, to records whose sex matches.
\item Rows whose basis is a change or multiple of baseline (creatinine, eGFR,
hemoglobin change) and the fasting or random glucose rows need columns
this function does not take; they are left \code{NA} and reported.
\item A test that \code{lParameterValues} does not map is left \code{NA} silently.
}

Nothing is dropped or aggregated: the input frame comes back with three
columns added, in its original row order. Where an established grading
system applies (DAIDS, CTCAE) the guide allows it in place of Tables 56 and
57 (\code{FDA-RULE-003}); pass it as \code{dfCriteria} in the same shape.
}
\examples{
dfLabs <- ExampleData("adbds")
dfLabs <- Derive_AbnormalityLevel(dfLabs)
dfGraded <- dfLabs[!is.na(dfLabs$AbnormalityLevel) & dfLabs$AbnormalityLevel > 0, ]
table(dfGraded$TEST, dfGraded$AbnormalityLevel)

}
\seealso{
\link{FDA_AbnormalityLevels} for the criteria and their transcription
notes; \code{\link[=Derive_ULNMultiple]{Derive_ULNMultiple()}} for the scale the ULN-multiple rows use.

Other FDA derivations:
\code{\link[=Derive_ExtremeValueFlag]{Derive_ExtremeValueFlag()}},
\code{\link[=Derive_ULNMultiple]{Derive_ULNMultiple()}}
}
\concept{FDA derivations}
