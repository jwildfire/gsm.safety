# Say how many records a derivation could not evaluate, and why

Say how many records a derivation could not evaluate, and why

## Usage

``` r
ReportSkipped(strWhat, lSkipped)
```

## Arguments

- strWhat:

  `character` What was being derived.

- lSkipped:

  Named `list` of `character` vectors: reason -\> parameters.
