% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Input_Participants.R
\name{Input_Participants}
\alias{Input_Participants}
\title{Study-level count of the participants a domain names}
\usage{
Input_Participants(
  dfDomain,
  dfSubjects,
  strIDCol = "subjid",
  strFilterCol = NULL,
  strFilterValues = NULL,
  strDomainName = "dfDomain",
  strGroupCol = "studyid",
  strGroupLevel = "Study"
)
}
\arguments{
\item{dfDomain}{\code{data.frame} Mapped domain to count participants from.}

\item{dfSubjects}{\code{data.frame} Mapped subject-level domain, the enrolled
population — \code{gsm.mapping}'s \code{Mapped_SUBJ}. It is both the anchor and the
denominator.}

\item{strIDCol}{\code{character} Participant ID column, shared by both domains.
Default: \code{"subjid"}.}

\item{strFilterCol}{\code{character} or \code{NULL} Column of \code{dfDomain} that decides
whether a participant counts. \code{NULL} (the default) counts every
participant the domain names.}

\item{strFilterValues}{\code{character} or \code{NULL} Values of \code{strFilterCol} that
count, compared without regard to case or surrounding space. Several are
written comma-separated — \code{"Y, N"} — following the ecosystem's convention
for a multi-valued meta scalar. \code{NULL} (the default) counts a participant
whose \code{strFilterCol} records \emph{anything}: not missing, not blank.}

\item{strDomainName}{\code{character} Name of the mapped domain, used in the
errors a reader has to act on. Default: \code{"dfDomain"}.}

\item{strGroupCol}{\code{character} Grouping column in \code{dfSubjects}. Default:
\code{"studyid"} — the study level this release publishes.}

\item{strGroupLevel}{\code{character} Group level to record. Default: \code{"Study"}.
Moving a metric to site level is this argument plus \code{strGroupCol}, not a
different metric.}
}
\value{
\code{data.frame} conforming to \code{analyticsInput} (\code{SubjectID}, \code{GroupID},
\code{GroupLevel}, \code{Numerator}, \code{Denominator}, \code{Metric}), one row per enrolled
participant — or zero rows when nothing was measured.
}
\description{
The numerator step behind most of the census metrics: \code{saf0005} and
\code{saf0007}, \code{saf0010} through \code{saf0015}. It counts the participants a mapped
domain names — optionally only those whose column matches a value, or whose
column records anything at all — anchored to the enrolled population, and
hands \code{gsm.core::Transform_Rate()} an ordinary \code{analyticsInput} frame.
}
\details{
One step, eight metrics. The differences between "participants with a lab
result" and "participants who completed" are which domain is read, which
column is looked at and which value is matched, and all three are declared
in the metric definition. Writing the arithmetic once is deliberate: eight
hand-written counting steps are eight chances to write one of them wrong,
which is the defect this rebuild exists to remove.
}
\section{Absent, empty, and zero are three different answers}{
\tabular{lll}{
   State \tab What the study said \tab What this returns \cr
   Absent \tab no domain, or no declared column in it \tab an \strong{error} \cr
   Empty \tab a domain with no rows at all \tab \strong{no row}, and a warning \cr
   Zero \tab a populated domain naming nobody \tab a row reading \strong{0} \cr
}


A zero is a measurement — the domain ran and the answer was none. Absence is
not a measurement, and neither is a table with nothing in it, so neither is
allowed to render as a reassuring zero. \code{gsm.core::CheckSpec()} only \emph{warns}
on a declared column that is missing (it errors only on a missing
data.frame), so the column check lives here for the guarantee to hold.

A subject domain with no rows is a fourth state: no denominator at all.
Nothing is published, and a warning says why.
}

\section{Counted once, and only if enrolled}{


Two rules the D0023 design makes standing, applied here rather than trusted:
the domain is reduced to one row per participant before counting, so a
duplicated row cannot inflate the figure; and the count is anchored to
\code{dfSubjects}, so a participant identifier that appears in the domain but not
in the enrolled population is never counted and the numerator can never
exceed its denominator.
}

\examples{
dfSubjects <- data.frame(subjid = c("S1", "S2"), studyid = "AA-AA-000-0000")
dfDisposition <- data.frame(subjid = c("S1", "S2"), compyn = c("Y", "N"))

# Participants with a disposition record
Input_Participants(dfDisposition, dfSubjects)

# Participants who completed
Input_Participants(
  dfDisposition, dfSubjects,
  strFilterCol = "compyn", strFilterValues = "Y"
)

}
