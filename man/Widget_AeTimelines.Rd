% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Widget_AeTimelines.R
\name{Widget_AeTimelines}
\alias{Widget_AeTimelines}
\title{AE Timelines Widget}
\usage{
Widget_AeTimelines(
  dfResults,
  lSettings = list(),
  width = NULL,
  height = NULL,
  elementId = NULL,
  bDebug = FALSE
)
}
\arguments{
\item{dfResults}{\code{data.frame} Adverse-event data, one record per event.
Column names are supplied by \code{lSettings}; the defaults expect
\code{USUBJID}/\code{AESEQ}/\code{ASTDY}/\code{AENDY}/\code{AETERM}/\code{AESEV} (see
\code{inst/schema/ae-timelines.json}).}

\item{lSettings}{\code{list} safety.viz ae-timelines settings overrides; merged
onto the module's \code{DEFAULT_SETTINGS} client-side, so only overrides are
needed.}

\item{width}{\code{character} Width of the widget as a CSS unit. Default: \code{NULL}.}

\item{height}{\code{character} Height of the widget as a CSS unit. Default:
\code{NULL}.}

\item{elementId}{\code{character} ID of the widget's HTML element. Default:
\code{NULL}.}

\item{bDebug}{\code{logical} Print debug messages in the browser console?
Default: \code{FALSE}.}
}
\description{
A widget that renders the safety.viz adverse-event timelines: one row per
participant, each event drawn from onset to resolution study day, colored by
severity (or another stratifier), with optional serious-event highlighting,
filters, and a participant detail listing.
}
\examples{
dfResults <- ExampleData("adae")

Widget_AeTimelines(
  dfResults,
  lSettings = list(
    color = list(
      value_col = "AESEV",
      label = "Severity"
    ),
    highlight = list(
      value_col = "AESER",
      label = "Serious Event",
      value = "Y"
    ),
    filters = list(
      list(value_col = "AESER", label = "Serious Event"),
      list(value_col = "AESEV", label = "Severity"),
      list(value_col = "ARM", label = "Treatment Group"),
      list(value_col = "USUBJID", label = "Participant ID")
    ),
    details = list(
      list(value_col = "AEBODSYS", label = "Body System"),
      list(value_col = "AEDECOD", label = "Dictionary-Derived Term")
    ),
    sort_participants = "earliest"
  )
)

}
