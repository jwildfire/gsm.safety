% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Input_QtProlongation.R
\name{Input_QtProlongation}
\alias{Input_QtProlongation}
\title{Participant-level QTc prolongation tier from ECG data}
\usage{
Input_QtProlongation(
  dfECG,
  strIDCol = "subjid",
  strMeasureCol = "egtstnam",
  strValueCol = "egstresn",
  strBaselineCol = "egbase",
  strChangeCol = "egchg",
  strBaselineFlagCol = "egblfl",
  strMeasure = "QTcF",
  vAbsoluteThresholds = c(450, 480, 500),
  vChangeThresholds = c(30, 60),
  strGroupLevel = "Subject"
)
}
\arguments{
\item{dfECG}{\code{data.frame} Long-format ECG records, one row per participant
per parameter per visit.}

\item{strIDCol}{\code{character} Participant ID column. Default: \code{"subjid"}.}

\item{strMeasureCol}{\code{character} Column holding the ECG parameter name.
Default: \code{"egtstnam"}.}

\item{strValueCol}{\code{character} Numeric result column. Default: \code{"egstresn"}.}

\item{strBaselineCol}{\code{character} Column holding the participant's baseline
value. Default: \code{"egbase"}.}

\item{strChangeCol}{\code{character} Column holding change from baseline; when
absent or non-numeric for a row, change is derived as value − baseline.
Default: \code{"egchg"}.}

\item{strBaselineFlagCol}{\code{character} Baseline-record flag column, used to
drop baseline records from scoring. Default: \code{"egblfl"}.}

\item{strMeasure}{\code{character} The corrected-QT parameter to score.
Default: \code{"QTcF"}.}

\item{vAbsoluteThresholds}{\code{numeric} Ascending absolute cut-points in ms.
Default: \code{c(450, 480, 500)}.}

\item{vChangeThresholds}{\code{numeric} Ascending change-from-baseline cut-points
in ms. Default: \code{c(30, 60)}.}

\item{strGroupLevel}{\code{character} Group level to record. Default: \code{"Subject"}.}
}
\value{
\code{data.frame} conforming to \code{analyticsInput}, one row per participant
with at least one post-baseline value, carrying the max absolute value, the
max change and the baseline as evidence columns.
}
\description{
Scores every participant with a post-baseline QTc against the ICH E14
outlier cut-points the \code{qt_explorer} chart already draws, taken from
\code{inst/schema/qt-explorer.json} (\verb{absolute_thresholds: [450, 480, 500]},
\verb{change_thresholds: [30, 60]}). Both criteria matter: on the demo study the
absolute cut alone flags one participant while the change cut flags ten, so a
metric built on absolute values alone would have looked healthy and said
nothing.
}
\details{
The tier ladder:\tabular{ll}{
   Tier \tab Rule \cr
   0 \tab no post-baseline value at the lowest absolute cut and no change at the lowest change cut \cr
   1 \tab max QTc at the first absolute cut (450 ms) \emph{or} max change at the first change cut (30 ms) \cr
   2 \tab max QTc at the second absolute cut (480 ms) \cr
   3 \tab max QTc at the third absolute cut (500 ms) \emph{or} max change at the second change cut (60 ms) \cr
}


Tier 3 is the conventional ICH E14 outlier pair (>500 ms or >60 ms change).
Only post-baseline records are scored — a baseline value is the reference,
not a finding — and a participant with a baseline but no post-baseline ECG
gets no row, because an unmeasured interval is not a normal one.
}
\examples{
dfECG <- data.frame(
  subjid = c("S1", "S1", "S2", "S2"),
  egtstnam = "QTcF",
  egstresn = c(400, 505, 390, 415),
  egbase = c(400, 400, 390, 390),
  egchg = c(0, 105, 0, 25),
  egblfl = c("Y", "", "Y", "")
)
Input_QtProlongation(dfECG)

}
