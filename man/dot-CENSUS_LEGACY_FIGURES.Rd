% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/SafetyCensus.R
\docType{data}
\name{.CENSUS_LEGACY_FIGURES}
\alias{.CENSUS_LEGACY_FIGURES}
\title{The \code{Census} table: which metric each named figure reads, and where it sits}
\format{
An object of class \code{data.frame} with 10 rows and 4 columns.
}
\usage{
.CENSUS_LEGACY_FIGURES
}
\description{
The labels are a contract (D0023.5, approved). The safety overview picks
\verb{Enrolled participants}, \verb{Received study drug}, \verb{Person-years on treatment}
and \code{Deaths} out of this payload by exact string and drops a tile silently
if one is reworded, so the labels stay word for word and a test says so.
}
\details{
\code{Denominator} is whether the figure is published beside one \emph{here}. Every
metric publishes a denominator, and the report shows it in a column headed
with what it counts; this payload has no such column, and its reader turns
any denominator into a percentage. "73.2 person-years of 762 (10\%)" is what
carrying it through would produce, so the figures with no percentage to
state carry no denominator, exactly as before.
}
\keyword{internal}
