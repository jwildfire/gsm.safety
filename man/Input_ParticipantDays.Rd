% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Input_ParticipantDays.R
\name{Input_ParticipantDays}
\alias{Input_ParticipantDays}
\title{Study-level total of participant-days}
\usage{
Input_ParticipantDays(
  dfSubjects,
  strDayCol,
  strIDCol = "subjid",
  strDomainName = "dfSubjects",
  strGroupCol = "studyid",
  strGroupLevel = "Study"
)
}
\arguments{
\item{dfSubjects}{\code{data.frame} Mapped subject-level domain, the enrolled
population — \code{gsm.mapping}'s \code{Mapped_SUBJ}.}

\item{strDayCol}{\code{character} Column of \code{dfSubjects} holding person-time in
days, e.g. \code{"timeonstudy"} or \code{"timeontreatment"}.}

\item{strIDCol}{\code{character} Participant ID column. Default: \code{"subjid"}.}

\item{strDomainName}{\code{character} Name of the mapped domain, used in the
errors a reader has to act on. Default: \code{"dfSubjects"}.}

\item{strGroupCol}{\code{character} Grouping column in \code{dfSubjects}. Default:
\code{"studyid"}.}

\item{strGroupLevel}{\code{character} Group level to record. Default: \code{"Study"}.}
}
\value{
\code{data.frame} conforming to \code{analyticsInput} (\code{SubjectID}, \code{GroupID},
\code{GroupLevel}, \code{Numerator}, \code{Denominator}, \code{Metric}), one row per
contributing participant — or zero rows when nothing was measured.
}
\description{
The numerator step behind \code{saf0008} and \code{saf0009}, the two person-time
figures. It sums a day column of the enrolled population — \code{timeonstudy} or
\code{timeontreatment} — and hands \code{gsm.core::Transform_Rate()} an ordinary
\code{analyticsInput} frame whose \code{Numerator} is the total days and whose
\code{Denominator} is the participants who contributed them.
}
\section{Days, not years}{


The figure published is the number actually summed, in the unit the subject
domain records it in. The census report is where person-years are presented,
and it divides. A metric that published years would be publishing a figure
nothing in the pipeline can check against the domain it came from.
}

\section{Missing person-time is not zero days}{


\code{Numerator / Denominator} is the mean days per participant, so a participant
whose person-time was never recorded cannot stay in one side of the fraction
and leave the other: counting them as zero days would understate the total
and inflate the denominator at once. They leave both, and a warning names
them. A \emph{recorded} zero is different — it is a measurement, and stays.

Negative person-time is treated the same way. It is not a duration, so it is
not summed, and the warning says how many participants it happened to.
}

\examples{
dfSubjects <- data.frame(
  subjid = c("S1", "S2"),
  studyid = "AA-AA-000-0000",
  timeonstudy = c(120, 90)
)
Input_ParticipantDays(dfSubjects, "timeonstudy")

}
