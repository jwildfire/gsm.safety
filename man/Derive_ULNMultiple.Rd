% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Derive_ULNMultiple.R
\name{Derive_ULNMultiple}
\alias{Derive_ULNMultiple}
\title{Express a result as a multiple of its upper limit of normal}
\usage{
Derive_ULNMultiple(
  dfResults,
  strValueCol = "STRESN",
  strULNCol = "STNRHI",
  strOutCol = "ULNMultiple"
)
}
\arguments{
\item{dfResults}{\code{data.frame} Long-format results, one row per participant
per test per visit, in the shape of \code{ExampleData("adbds")}.}

\item{strValueCol}{\code{character} Numeric result column. Default: \code{"STRESN"}.}

\item{strULNCol}{\code{character} Upper-limit-of-normal column. Default:
\code{"STNRHI"}.}

\item{strOutCol}{\code{character} Name of the column to add. Default:
\code{"ULNMultiple"}. Must not already be a column of \code{dfResults}: the
function appends, it never overwrites.}
}
\value{
\code{dfResults} with \code{strOutCol} appended: \code{numeric}, the result as a
multiple of ULN, \code{NA} where it cannot be formed.
}
\description{
Adds one numeric column: the result divided by the record's own upper limit
of normal. This is the scale the FDA Standard Safety Tables and Figures
Integrated Guide (v2.0) draws the DILI screening plots on (Figures 7 and 8,
section 2.4.4) and the scale every \verb{x ULN} threshold in its Appendix Tables
56 and 57 is read on. The guide normalises to the \emph{reported} ULN because
normal ranges vary between laboratory sites, so the denominator is the
record's own, never a study-wide constant. Requirement \code{FDA-RULE-001} in
\code{requirements/fda-stf.md}.
}
\details{
The multiple is \code{NA} where the result is missing or the ULN is missing,
non-numeric or not positive. Nothing is dropped and nothing is aggregated:
the input frame comes back with the column added, in its original row
order (the design's L1 contract). \code{\link[=Input_HysLaw]{Input_HysLaw()}} computes the same
quantity internally for its peak-per-participant reduction; the two agree
record for record.
}
\examples{
dfLabs <- ExampleData("adbds")
dfLabs <- Derive_ULNMultiple(dfLabs)
dfALT <- dfLabs[dfLabs$TEST == "Alanine Aminotransferase", ]
head(dfALT[, c("USUBJID", "VISIT", "STRESN", "STNRHI", "ULNMultiple")])

}
\seealso{
\code{\link[=Derive_AbnormalityLevel]{Derive_AbnormalityLevel()}}, which grades the multiple against
Tables 56 and 57; \code{\link[=Input_HysLaw]{Input_HysLaw()}}, which reduces it to a peak per
participant.

Other FDA derivations:
\code{\link[=Derive_AbnormalityLevel]{Derive_AbnormalityLevel()}},
\code{\link[=Derive_ExtremeValueFlag]{Derive_ExtremeValueFlag()}}
}
\concept{FDA derivations}
