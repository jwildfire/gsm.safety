% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Widget_Histogram.R
\name{Widget_Histogram}
\alias{Widget_Histogram}
\title{Histogram Widget}
\usage{
Widget_Histogram(
  dfResults,
  lSettings = list(),
  width = NULL,
  height = NULL,
  elementId = NULL,
  bDebug = FALSE
)
}
\arguments{
\item{dfResults}{\code{data.frame} Long-format results data, one record per
measurement. Column names are supplied by \code{lSettings}; the defaults
expect \code{TEST}/\code{STRESN}/\code{USUBJID}/\code{STRESU}/\code{STNRLO}/\code{STNRHI} (see
\code{inst/schema/histogram.json}).}

\item{lSettings}{\code{list} safety.viz histogram settings overrides; merged
onto the module's \code{DEFAULT_SETTINGS} client-side, so only overrides are
needed.}

\item{width}{\code{character} Width of the widget as a CSS unit. Default: \code{NULL}.}

\item{height}{\code{character} Height of the widget as a CSS unit. Default:
\code{NULL}.}

\item{elementId}{\code{character} ID of the widget's HTML element. Default:
\code{NULL}.}

\item{bDebug}{\code{logical} Print debug messages in the browser console?
Default: \code{FALSE}.}
}
\description{
A widget that renders the safety.viz histogram: the distribution of
long-format safety results (labs, vitals) for a selected measure, with
optional filters, group-by small multiples, normal-range annotations, and
distribution comparison tests.
}
\examples{
dfResults <- ExampleData("adbds")

Widget_Histogram(
  dfResults,
  lSettings = list(
    filters = list(
      list(value_col = "SITEID", label = "Site ID"),
      list(value_col = "SEX", label = "Sex"),
      list(value_col = "RACE", label = "Race"),
      list(value_col = "ARM", label = "Treatment Group"),
      list(value_col = "USUBJID", label = "Participant ID")
    ),
    groups = list(
      list(value_col = "SITE", label = "Site"),
      list(value_col = "SEX", label = "Sex"),
      list(value_col = "RACE", label = "Race"),
      list(value_col = "ARM", label = "Treatment Group")
    ),
    display_normal_range = TRUE,
    annotate_bin_boundaries = TRUE,
    test_normality = TRUE,
    group_by = "ARM",
    compare_distributions = TRUE
  )
)

}
