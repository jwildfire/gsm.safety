% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Widget_DeltaDelta.R
\name{Widget_DeltaDelta}
\alias{Widget_DeltaDelta}
\title{Delta-Delta Widget}
\usage{
Widget_DeltaDelta(
  dfResults,
  lSettings = list(),
  width = NULL,
  height = NULL,
  elementId = NULL,
  bDebug = FALSE
)
}
\arguments{
\item{dfResults}{\code{data.frame} Long-format results data, one record per
measurement at a visit. Column names are supplied by \code{lSettings}; the
defaults expect \code{TEST}/\code{STRESN}/\code{USUBJID}/\code{VISIT}/\code{VISITNUM} (see
\code{inst/schema/delta-delta.json}).}

\item{lSettings}{\code{list} safety.viz delta-delta settings overrides; merged
onto the module's \code{DEFAULT_SETTINGS} client-side, so only overrides are
needed.}

\item{width}{\code{character} Width of the widget as a CSS unit. Default: \code{NULL}.}

\item{height}{\code{character} Height of the widget as a CSS unit. Default:
\code{NULL}.}

\item{elementId}{\code{character} ID of the widget's HTML element. Default:
\code{NULL}.}

\item{bDebug}{\code{logical} Print debug messages in the browser console?
Default: \code{FALSE}.}
}
\description{
A widget that renders the safety.viz delta-delta plot: change from baseline
in one measure versus change from baseline in another, one point per
participant, with baseline/comparison visit selectors, optional filters, and
an optional linear regression line.
}
\examples{
dfResults <- ExampleData("adbds")

Widget_DeltaDelta(
  dfResults,
  lSettings = list(
    filters = list(
      list(value_col = "SITE", label = "Site"),
      list(value_col = "SEX", label = "Sex"),
      list(value_col = "RACE", label = "Race"),
      list(value_col = "ARM", label = "Treatment Group"),
      list(value_col = "USUBJID", label = "Participant ID")
    ),
    measure_x = "Alanine Aminotransferase",
    measure_y = "Aspartate Aminotransferase",
    add_regression_line = TRUE
  )
)

}
