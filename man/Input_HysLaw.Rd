% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Input_HysLaw.R
\name{Input_HysLaw}
\alias{Input_HysLaw}
\title{Participant-level Hy's Law candidate tier from liver chemistry}
\usage{
Input_HysLaw(
  dfLabs,
  strIDCol = "subjid",
  strMeasureCol = "lbtstnam",
  strValueCol = "lbstresn",
  strULNCol = "lbstnrhi",
  lMeasureValues = list(ALT = "Alanine Aminotransferase", AST =
    "Aspartate Aminotransferase", TB = "Bilirubin", ALP = "Alkaline Phosphatase"),
  nAminotransferaseCut = 3,
  nBilirubinCut = 2,
  nCholestaticCut = 2,
  strGroupLevel = "Subject"
)
}
\arguments{
\item{dfLabs}{\code{data.frame} Long-format lab results, one row per participant
per measure per visit.}

\item{strIDCol}{\code{character} Participant ID column. Default: \code{"subjid"}.}

\item{strMeasureCol}{\code{character} Column holding the measure name.
Default: \code{"lbtstnam"}.}

\item{strValueCol}{\code{character} Numeric result column. Default: \code{"lbstresn"}.}

\item{strULNCol}{\code{character} Upper-limit-of-normal column, the denominator of
the ×ULN standardisation. Default: \code{"lbstnrhi"}.}

\item{lMeasureValues}{\code{list} Map of the measure keys \code{ALT}, \code{AST}, \code{TB} and
\code{ALP} to the measure strings in the data — the same mapping the chart's
\code{measure_values} setting takes.}

\item{nAminotransferaseCut}{\code{numeric} ×ULN cut for ALT / AST. Default: \code{3}.}

\item{nBilirubinCut}{\code{numeric} ×ULN cut for total bilirubin. Default: \code{2}.}

\item{nCholestaticCut}{\code{numeric} ×ULN cut at or above which the pattern is
read as cholestatic, holding the participant at tier 2. Default: \code{2}.}

\item{strGroupLevel}{\code{character} Group level to record. Default: \code{"Subject"}.}
}
\value{
\code{data.frame} conforming to \code{analyticsInput}, one row per participant
with at least one usable liver result, carrying the peak ×ULN of each
measure and the R ratio as evidence columns.
}
\description{
Scores every participant with liver chemistry against the eDISH quadrants the
\code{hep_explorer} chart already draws, using that chart's own cut-points from
\code{inst/schema/hep-explorer.json} (\code{cuts.defaults.relative_uln} = 3 for the
aminotransferases, \code{cuts.TB.relative_uln} = 2 for total bilirubin). Each
participant is reduced to their \strong{peak} ×ULN value per measure — the same
one-point-per-participant reduction the chart performs — so a flag and the
point a reviewer sees on the chart are the same observation.
}
\details{
The tier ladder, ordered by how much review the participant warrants:\tabular{lll}{
   Tier \tab eDISH region \tab Rule \cr
   0 \tab normal \tab neither axis at its cut \cr
   1 \tab Temple's Corollary \emph{or} hyperbilirubinaemia \tab exactly one axis at its cut \cr
   2 \tab both axes, cholestatic pattern \tab both axes at their cut, peak ALP at or above the cholestatic cut \cr
   3 \tab \strong{potential Hy's Law} \tab both axes at their cut, peak ALP below the cholestatic cut \cr
}


Tier 3 is a \emph{candidate}, not an adjudicated Hy's Law case. Two standard
criteria are deliberately out of scope here and belong to case review: the
requirement that the aminotransferase and bilirubin elevations be temporally
associated (peaks are taken independently, exactly as eDISH does), and the
exclusion of other causes of liver injury.
}
\examples{
dfLabs <- data.frame(
  subjid = rep(c("S1", "S2"), each = 4),
  lbtstnam = rep(c("ALT", "AST", "TB", "ALP"), 2),
  lbstresn = c(150, 120, 60, 90, 20, 25, 8, 80),
  lbstnrhi = c(40, 40, 20, 120, 40, 40, 20, 120)
)
Input_HysLaw(
  dfLabs,
  lMeasureValues = list(ALT = "ALT", AST = "AST", TB = "TB", ALP = "ALP")
)

}
