% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Input_SafetyAE.R
\name{Input_SafetyAE}
\alias{Input_SafetyAE}
\title{Participant-level serious / related / discontinuation AE tier}
\usage{
Input_SafetyAE(
  dfAE,
  strIDCol = "subjid",
  strSeriousCol = "aeser",
  strRelatedCol = "aerel",
  strGradeCol = "aetoxgr",
  strActionCol = NULL,
  chrYesValues = c("Y", "YES", "TRUE", "1"),
  chrDiscontinuationValues = c("DRUG WITHDRAWN", "DISCONTINUED",
    "PERMANENTLY DISCONTINUED"),
  nSeriousRelatedGrade = 4,
  dfSubjects = NULL,
  strSubjectIDCol = "subjid",
  strGroupLevel = "Subject"
)
}
\arguments{
\item{dfAE}{\code{data.frame} Long-format adverse events, one row per event.}

\item{strIDCol}{\code{character} Participant ID column. Default: \code{"subjid"}.}

\item{strSeriousCol}{\code{character} Seriousness flag column. Default: \code{"aeser"}.}

\item{strRelatedCol}{\code{character} Causality / relatedness flag column.
Default: \code{"aerel"}.}

\item{strGradeCol}{\code{character} Numeric severity grade column (CTCAE).
Default: \code{"aetoxgr"}.}

\item{strActionCol}{\code{character} or \code{NULL} Action-taken column; the
discontinuation leg reads this. Default: \code{NULL} — inactive.}

\item{chrYesValues}{\code{character} Values of the flag columns read as "yes".
Default: \code{c("Y", "YES", "TRUE", "1")}.}

\item{chrDiscontinuationValues}{\code{character} Values of \code{strActionCol} read as
study-drug discontinuation.
Default: \code{c("DRUG WITHDRAWN", "DISCONTINUED", "PERMANENTLY DISCONTINUED")}.}

\item{nSeriousRelatedGrade}{\code{numeric} Severity grade at or above which a
serious, related AE reaches tier 3. Default: \code{4}.}

\item{dfSubjects}{\code{data.frame} or \code{NULL} Subject-level domain. When supplied,
every enrolled participant is scored, so a participant with no AE at all is
an explicit tier 0 rather than an absent row — for this metric absence of
events is a real finding, not missing data. Default: \code{NULL}.}

\item{strSubjectIDCol}{\code{character} Participant ID column in \code{dfSubjects}.
Default: \code{"subjid"}.}

\item{strGroupLevel}{\code{character} Group level to record. Default: \code{"Subject"}.}
}
\value{
\code{data.frame} conforming to \code{analyticsInput}, one row per participant,
carrying the counts behind the tier as evidence columns.
}
\description{
Scores every participant against the seriousness, causality and action-taken
fields the \code{ae_explorer} and \code{ae_timelines} charts already filter on.
}
\details{
Unlike the liver and QT metrics, this one has no published cut-point to
inherit — seriousness and relatedness are flags, not scales — so the tier
ladder is the design decision, and it is the difference between a review
queue and a list of most of the study. Measured on the demo study, "any
serious AE" reaches 41.7\% of participants and "any related AE" 67.8\%: neither
triages anything. Severity is what separates them:\tabular{ll}{
   Tier \tab Rule \cr
   0 \tab no serious, related or discontinuation AE \cr
   1 \tab a related AE, not serious \cr
   2 \tab a serious AE, or an AE whose action taken was study-drug discontinuation \cr
   3 \tab a serious \strong{and} related AE at or above the severity grade cut \cr
}


Tier 3 defaults to CTCAE grade 4 (life-threatening), the shape of an expedited
safety report: serious, related, and severe enough that the reviewer's
question is causality rather than triage.

The discontinuation leg is only as good as the source column. \code{strActionCol}
names the AE action-taken field; when it is \code{NULL} or absent from the data,
that leg is inactive and the returned frame records \code{ActionColumnPresent = FALSE}, so a study missing the column reports the gap rather than silently
scoring as though no participant ever discontinued.
}
\examples{
dfAE <- data.frame(
  subjid = c("S1", "S1", "S2"),
  aeser = c("Y", "N", "N"),
  aerel = c("Y", "Y", "Y"),
  aetoxgr = c(4, 1, 2)
)
Input_SafetyAE(dfAE)

}
