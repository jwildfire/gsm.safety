% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Widget_QtExplorer.R
\name{Widget_QtExplorer}
\alias{Widget_QtExplorer}
\title{QT Safety Explorer Widget}
\usage{
Widget_QtExplorer(
  dfResults,
  lSettings = list(),
  width = NULL,
  height = NULL,
  elementId = NULL,
  bDebug = FALSE
)
}
\arguments{
\item{dfResults}{\code{data.frame} Long-format ECG results, one record per
measurement. Column names are supplied by \code{lSettings}; the defaults expect
the ADaM ADEG standard (\code{TEST}/\code{STRESN}/\code{BASE}/\code{ARM}, plus
\code{VISIT}/\code{VISITNUM}/\code{ABLFL} — see \code{inst/schema/qt-explorer.json}), so data
using those names needs no settings at all. The \code{measures} setting selects
which \code{TEST} values are charted and \code{qtc_measures} marks which of them are
QTc intervals.}

\item{lSettings}{\code{list} safety.viz qt-explorer settings overrides; merged
onto the module's \code{DEFAULT_SETTINGS} client-side, so only overrides are
needed. Set \code{placebo_arm} to enable the placebo-corrected (ΔΔ) view.}

\item{width}{\code{character} Width of the widget as a CSS unit. Default: \code{NULL}.}

\item{height}{\code{character} Height of the widget as a CSS unit. Default:
\code{NULL}.}

\item{elementId}{\code{character} ID of the widget's HTML element. Default:
\code{NULL}.}

\item{bDebug}{\code{logical} Print debug messages in the browser console?
Default: \code{FALSE}.}
}
\description{
A widget that renders the safety.viz QT/QTc safety explorer across three
linked views: central tendency (mean change from baseline and
placebo-corrected change with confidence intervals, against the ICH E14
10 ms threshold of regulatory concern), an outlier scatter of absolute
values and changes against the 450/480/500 ms and 30/60 ms category
boundaries, and a categorical summary of participants crossing them.
}
\examples{
dfResults <- ExampleData("adeg")

# The bundled ADEG data uses the default column names, so the chart
# initializes with no settings.
Widget_QtExplorer(dfResults)

# Name the placebo arm to add the placebo-corrected change (ΔΔ) view.
Widget_QtExplorer(
  dfResults,
  lSettings = list(
    placebo_arm = "Placebo",
    filters = list(
      list(value_col = "SEX", label = "Sex"),
      list(value_col = "RACE", label = "Race"),
      list(value_col = "SITE", label = "Site")
    )
  )
)

}
