# The default map from Table 58 to 60 parameters to `ExampleData("adbds")` test names

The default map from Table 58 to 60 parameters to `ExampleData("adbds")`
test names

## Usage

``` r
DefaultExtremeParameters()
```

## Value

Named `list` of `character`.
