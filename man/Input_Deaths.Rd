% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Input_Deaths.R
\name{Input_Deaths}
\alias{Input_Deaths}
\title{Study-level count of participants who died}
\usage{
Input_Deaths(
  dfDeath,
  dfSubjects,
  strIDCol = "subjid",
  strDeathCol = "death",
  strGroupCol = "studyid",
  strGroupLevel = "Study"
)
}
\arguments{
\item{dfDeath}{\code{data.frame} Mapped death domain, one row per death record —
\code{gsm.mapping}'s \code{Mapped_Death}. Rows it carries for other reasons (a
progressive-disease date, say) have \code{death} missing rather than \code{TRUE}.}

\item{dfSubjects}{\code{data.frame} Mapped subject-level domain, the enrolled
population — \code{gsm.mapping}'s \code{Mapped_SUBJ}. It is both the anchor and the
denominator.}

\item{strIDCol}{\code{character} Participant ID column, shared by both domains.
Default: \code{"subjid"}.}

\item{strDeathCol}{\code{character} Column of \code{dfDeath} marking a death. Read as
true when logical \code{TRUE}, a non-zero number, or one of \code{"TRUE"}, \code{"T"},
\code{"Y"}, \code{"YES"}, \code{"1"}. Default: \code{"death"}.}

\item{strGroupCol}{\code{character} Grouping column in \code{dfSubjects}. Default:
\code{"studyid"} — the study level this release publishes.}

\item{strGroupLevel}{\code{character} Group level to record. Default: \code{"Study"}.
Moving the metric to site level is this argument plus \code{strGroupCol}, not a
different metric.}
}
\value{
\code{data.frame} conforming to \code{analyticsInput} (\code{SubjectID}, \code{GroupID},
\code{GroupLevel}, \code{Numerator}, \code{Denominator}, \code{Metric}), one row per enrolled
participant — or zero rows when the death domain is empty.
}
\description{
The numerator step behind \code{saf0004}, the first of the census metrics. It
counts the participants a study's \strong{death domain} records as having died,
anchored to the enrolled population, and hands
\code{gsm.core::Transform_Rate()} an ordinary \code{analyticsInput} frame.
}
\details{
No clinical definition of death is written here. \code{Mapped_Death} is
gsm.mapping's own union of the death domain and the study-completion
domain's discontinuation reason (\code{gsm.mapping::complete_death()}); this
function counts the participants that union marks, and nothing else.
}
\section{Absent, empty, and zero are three different answers}{


The defect this metric replaces reported \strong{one} death on a study whose
records hold thirteen, by text-matching a discontinuation reason. The defect
beside it reported \strong{zero} where a column was missing. So the three states
are kept apart deliberately:\tabular{lll}{
   State \tab What the study said \tab What this returns \cr
   Absent \tab no death domain, or no \code{death} / ID column in it \tab an \strong{error} \cr
   Empty \tab a death domain with no rows at all \tab \strong{no row}, and a warning \cr
   Zero \tab a populated death domain marking nobody as died \tab a row reading \strong{0} \cr
}


A zero is a measurement — the domain ran and the answer was none. Absence is
not a measurement, and neither is a table with nothing in it, so neither is
allowed to render as a reassuring zero. Note that \code{gsm.core::CheckSpec()}
only \emph{warns} on a declared column that is missing (it errors only on a
missing data.frame), so the column check has to live here for the guarantee
to hold.
}

\section{Counted once, and only if enrolled}{


Two rules the D0023 design makes standing, applied here rather than trusted:
the death domain is reduced to one row per participant before counting, so a
duplicated row cannot inflate the figure; and the count is anchored to
\code{dfSubjects}, so a participant identifier that appears in the death domain
but not in the enrolled population is never counted and the numerator can
never exceed its denominator.
}

\examples{
dfSubjects <- data.frame(subjid = c("S1", "S2"), studyid = "AA-AA-000-0000")
dfDeath <- data.frame(subjid = c("S1", "S2"), death = c(TRUE, NA))
Input_Deaths(dfDeath, dfSubjects)

}
