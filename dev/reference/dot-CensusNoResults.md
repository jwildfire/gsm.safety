# What a study whose metrics all published nothing produces

What a study whose metrics all published nothing produces

## Usage

``` r
.CensusNoResults()
```

## Value

`data.frame` with the reporting-model columns and no rows.
