% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Widget_ShiftPlot.R
\name{Widget_ShiftPlot}
\alias{Widget_ShiftPlot}
\title{Shift Plot Widget}
\usage{
Widget_ShiftPlot(
  dfResults,
  lSettings = list(),
  width = NULL,
  height = NULL,
  elementId = NULL,
  bDebug = FALSE
)
}
\arguments{
\item{dfResults}{\code{data.frame} Long-format results data, one record per
participant per visit per measure. Column names are supplied by
\code{lSettings}; the defaults expect
\code{TEST}/\code{STRESN}/\code{VISIT}/\code{VISITNUM}/\code{USUBJID}/\code{STRESU} (see
\code{inst/schema/shift-plot.json}).}

\item{lSettings}{\code{list} safety.viz shift-plot settings overrides; merged
onto the module's \code{DEFAULT_SETTINGS} client-side, so only overrides are
needed.}

\item{width}{\code{character} Width of the widget as a CSS unit. Default: \code{NULL}.}

\item{height}{\code{character} Height of the widget as a CSS unit. Default:
\code{NULL}.}

\item{elementId}{\code{character} ID of the widget's HTML element. Default:
\code{NULL}.}

\item{bDebug}{\code{logical} Print debug messages in the browser console?
Default: \code{FALSE}.}
}
\description{
A widget that renders the safety.viz shift plot: a baseline-versus-comparison
scatter that pairs each participant's baseline-visit value against their
comparison-visit value for a selected measure, with optional filters,
summary-statistic controls, and a participant-level listing.
}
\examples{
dfResults <- ExampleData("adbds")

Widget_ShiftPlot(
  dfResults,
  lSettings = list(
    filters = list(
      list(value_col = "SITEID", label = "Site ID"),
      list(value_col = "SEX", label = "Sex"),
      list(value_col = "RACE", label = "Race"),
      list(value_col = "ARM", label = "Treatment Group")
    ),
    baseline_visits = list("Baseline"),
    comparison_visits = list("Week 26")
  )
)

}
