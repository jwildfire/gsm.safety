% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Widget_NepExplorer.R
\name{Widget_NepExplorer}
\alias{Widget_NepExplorer}
\title{Nephrotoxicity Explorer Widget}
\usage{
Widget_NepExplorer(
  dfResults,
  lSettings = list(),
  width = NULL,
  height = NULL,
  elementId = NULL,
  bDebug = FALSE
)
}
\arguments{
\item{dfResults}{\code{data.frame} Long-format lab results, one record per
measurement. Column names are supplied by \code{lSettings}; the defaults
expect the BDS standard (\code{USUBJID}/\code{TEST}/\code{STRESN}/\code{STRESU}, plus
\code{VISIT}/\code{VISITNUM} — see \code{inst/schema/nep-explorer.json}), so data using
those names needs no settings at all. Serum creatinine is selected by
\code{measure_values$CREAT} (default \code{"Creatinine"}).}

\item{lSettings}{\code{list} safety.viz nep-explorer settings overrides; merged
onto the module's \code{DEFAULT_SETTINGS} client-side, so only overrides are
needed.}

\item{width}{\code{character} Width of the widget as a CSS unit. Default: \code{NULL}.}

\item{height}{\code{character} Height of the widget as a CSS unit. Default:
\code{NULL}.}

\item{elementId}{\code{character} ID of the widget's HTML element. Default:
\code{NULL}.}

\item{bDebug}{\code{logical} Print debug messages in the browser console?
Default: \code{FALSE}.}
}
\description{
A widget that renders the safety.viz KDIGO nephrotoxicity explorer: one
point per participant at their maximum post-baseline fold change in serum
creatinine against their maximum absolute change, over the L-shaped KDIGO
acute-kidney-injury stage zones, with a stage summary table beside the
chart. Participants meeting the KDIGO >= 4.0 mg/dL Stage 3 criterion are
drawn as a distinct triangular mark rather than a zone. The renderer is
marked Experimental upstream pending clinical confirmation of the staging
ladder.
}
\examples{
dfResults <- ExampleData("adbds")

# The bundled BDS data uses the default column names, so the chart
# initializes with no settings. The synthetic `AKI-*` cohort supplies the
# staged participants; the pilot population alone stays in the no-stage box.
Widget_NepExplorer(dfResults)

# Add cohort filters.
Widget_NepExplorer(
  dfResults,
  lSettings = list(
    filters = list(
      list(value_col = "ARM", label = "Treatment Group"),
      list(value_col = "SEX", label = "Sex"),
      list(value_col = "RACE", label = "Race")
    )
  )
)

}
