% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Report_SafetyCensus.R
\name{Report_SafetyCensus}
\alias{Report_SafetyCensus}
\title{Render the census report}
\usage{
Report_SafetyCensus(
  dfFigures,
  dfCoverage = NULL,
  dfProvenance = NULL,
  dfResults = NULL,
  strTitle = "Safety Census",
  strOutputDir = getwd(),
  strOutputFile = NULL
)
}
\arguments{
\item{dfFigures}{\code{data.frame} From \code{\link[=Report_CensusFigures]{Report_CensusFigures()}}.}

\item{dfCoverage}{\code{data.frame} From \code{\link[=Report_CensusCoverage]{Report_CensusCoverage()}}. Zero rows —
or \code{NULL} — renders no coverage section at all, rather than an empty one.}

\item{dfProvenance}{\code{data.frame} From \code{\link[=Report_CensusProvenance]{Report_CensusProvenance()}}. \code{NULL}
renders no provenance section.}

\item{dfResults}{\code{data.frame} The metric results, read only for the study
identifier and snapshot date in the page header.}

\item{strTitle}{\code{character} Page title. Default: \code{"Safety Census"}.}

\item{strOutputDir}{\code{character} Directory to write into. Default:
\code{\link[=getwd]{getwd()}}.}

\item{strOutputFile}{\code{character} File name. Default: a name built from the
study and snapshot date.}
}
\value{
The path of the written page, invisibly.
}
\description{
Writes the safety census as one self-contained HTML page from the tables the
report workflow's other steps assembled. It is the last step of
\verb{inst/workflow/4_modules/safety_census.yaml}, and like every step before it,
it reads: it lays out figures it did not compute.
}
\section{What the page holds}{


Each section of figures the workflow declares, every figure beside the
denominator its own metric published; then the coverage table, \strong{only if a
coverage figure was published}; then what every metric published, verbatim,
including the metrics that published nothing.

Nothing on the page flags. There is no flag column, no colour carrying a
verdict and no cut-point — D0023.3, and the same rule the metrics follow by
declaring no threshold and publishing an empty flag.
}

\examples{
dfFigures <- data.frame(
  Section = "Population", ID = "saf0004", Figure = "Deaths (Study)",
  Value = 13, Unit = NA_character_, Denominator = 762,
  DenominatorLabel = "Enrolled Participant"
)
strPath <- Report_SafetyCensus(dfFigures, strOutputDir = tempdir())

}
