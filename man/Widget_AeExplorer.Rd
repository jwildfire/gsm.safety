% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Widget_AeExplorer.R
\name{Widget_AeExplorer}
\alias{Widget_AeExplorer}
\title{Adverse Event Explorer Widget}
\usage{
Widget_AeExplorer(
  dfResults,
  lSettings = list(),
  width = NULL,
  height = NULL,
  elementId = NULL,
  bDebug = FALSE
)
}
\arguments{
\item{dfResults}{\code{data.frame} Adverse-event data, one record per event, plus
one placeholder row per participant with no adverse events so the
population denominator covers the whole safety population. Placeholder
rows carry a blank System Organ Class and never render as events. Column
names are supplied by \code{lSettings}; the defaults expect the ADaM ADAE
standard (\code{USUBJID}/\code{AEBODSYS}/\code{AEDECOD}/\code{ARM} — see
\code{inst/schema/ae-explorer.json}), so data using those names needs no
settings at all.}

\item{lSettings}{\code{list} safety.viz ae-explorer settings overrides; merged
onto the module's \code{DEFAULT_SETTINGS} client-side, so only overrides are
needed.}

\item{width}{\code{character} Width of the widget as a CSS unit. Default: \code{NULL}.}

\item{height}{\code{character} Height of the widget as a CSS unit. Default:
\code{NULL}.}

\item{elementId}{\code{character} ID of the widget's HTML element. Default:
\code{NULL}.}

\item{bDebug}{\code{logical} Print debug messages in the browser console?
Default: \code{FALSE}.}
}
\description{
A widget that renders the safety.viz adverse-event explorer: a
System-Organ-Class / preferred-term prevalence table with per-arm rates and
a between-arm difference plot, expandable to the events behind any row and
filterable down to a participant listing.
}
\examples{
dfResults <- ExampleData("adae")

# The bundled ADAE data uses the default column names, so the chart
# initializes with no settings.
Widget_AeExplorer(dfResults)

# Add filters and a severity drill-down.
Widget_AeExplorer(
  dfResults,
  lSettings = list(
    filters = list(
      list(value_col = "AESEV", label = "Severity"),
      list(value_col = "AESER", label = "Serious")
    ),
    details = list(
      list(value_col = "AETERM", label = "Reported Term"),
      list(value_col = "AESEV", label = "Severity"),
      list(value_col = "ASTDY", label = "Start Day")
    )
  )
)

}
