% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Widget_HepWaterfall.R
\name{Widget_HepWaterfall}
\alias{Widget_HepWaterfall}
\title{Hepatic ALT Waterfall Widget}
\usage{
Widget_HepWaterfall(
  dfResults,
  lSettings = list(),
  width = NULL,
  height = NULL,
  elementId = NULL,
  bDebug = FALSE
)
}
\arguments{
\item{dfResults}{\code{data.frame} Long-format lab results, one record per
measurement, carrying at least the waterfall measure and total bilirubin
(for the jaundice overlay and the baseline-bilirubin cohort rule). Column
names are supplied by \code{lSettings} (see \code{inst/schema/hep-waterfall.json});
\code{measure_values} maps the liver-measure keys to the data's \code{TEST} values,
and naming \code{placebo_arm}/\code{active_arms} avoids relying on the arm-name
auto-detect.}

\item{lSettings}{\code{list} safety.viz hep-waterfall settings overrides; merged
onto the module's \code{DEFAULT_SETTINGS} client-side, so only overrides are
needed.}

\item{width}{\code{character} Width of the widget as a CSS unit. Default: \code{NULL}.}

\item{height}{\code{character} Height of the widget as a CSS unit. Default:
\code{NULL}.}

\item{elementId}{\code{character} ID of the widget's HTML element. Default:
\code{NULL}.}

\item{bDebug}{\code{logical} Print debug messages in the browser console?
Default: \code{FALSE}.}
}
\description{
A widget that renders the safety.viz modified ALT waterfall (Amirzadegan et
al., \emph{Drug Safety} 2025;48(5):443-453, Figure 5) for trials that enrol
participants with abnormal baseline liver tests: one floating bar per
participant from their baseline to their maximum on-treatment value in
absolute units, ranked by baseline so the two arms' highest baselines meet
at the seam, with new-onset jaundice overriding the arm colour and a
box-and-whisker summary flanking each arm. The renderer is marked a
prototype upstream pending open clinical judgement calls.
}
\examples{
# The bundled synthetic abnormal-baseline cohort; the pilot trial data
# cannot form this figure (see the safety.viz demo's data note).
dfResults <- ExampleData("adbds_abnbl")

Widget_HepWaterfall(
  dfResults,
  lSettings = list(
    studyday_col = "VISITNUM",
    measure_values = list(
      ALT = "Alanine Aminotransferase",
      AST = "Aspartate Aminotransferase",
      TB = "Bilirubin",
      ALP = "Alkaline Phosphatase"
    ),
    arm_col = "ARM",
    placebo_arm = "ABL: Placebo",
    active_arms = list("ABL: Study Drug")
  )
)

}
