# Fold the spellings of sex to `m` or `f`

Fold the spellings of sex to `m` or `f`

## Usage

``` r
NormaliseSex(chrSex)
```

## Arguments

- chrSex:

  `character` Sex values.

## Value

`character` `"m"`, `"f"` or `NA`.
