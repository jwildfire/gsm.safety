% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-metric.R
\name{MakeParticipantInput}
\alias{MakeParticipantInput}
\title{Assemble a participant-level \code{analyticsInput} frame}
\usage{
MakeParticipantInput(
  dfEvidence,
  strIDCol = "subjid",
  strGroupLevel = "Subject"
)
}
\arguments{
\item{dfEvidence}{\code{data.frame} One row per assessed participant, carrying at
least the participant ID column and a \code{Numerator} column holding the tier.}

\item{strIDCol}{\code{character} Name of the participant ID column in
\code{dfEvidence}. Default: \code{"subjid"}.}

\item{strGroupLevel}{\code{character} Group level to record. Default: \code{"Subject"}.}
}
\value{
\code{data.frame} with \code{SubjectID}, \code{GroupID}, \code{GroupLevel}, \code{Numerator},
\code{Denominator}, \code{Metric}, followed by every other column of \code{dfEvidence}.
}
\description{
The gsm analytics contract is \code{analyticsInput}
(\code{SubjectID}, \code{GroupID}, \code{GroupLevel}, \code{Numerator}, \code{Denominator},
\code{Metric}) in, \code{analyticsSummary} out. gsm.safety's metrics are
participant-level — \code{GroupLevel: Subject}, with \code{GroupID} equal to
\code{SubjectID}, following the \code{pat0015.yaml} precedent in gsm.kri — and each
one scores a participant with an ordinal \emph{tier} rather than a rate.
}
\details{
Every metric therefore emits one row per \strong{assessed} participant with
\code{Numerator} = the tier and \code{Denominator} = 1, so \code{gsm.core::Transform_Rate()}
carries the tier through to \code{Metric} unchanged and
\code{gsm.core::Analyze_Identity()} copies it to \code{Score}. A participant with no
assessable data gets no row at all: "not assessed" is absence, never a
reassuring zero. The row count is the metric's own coverage denominator.

Evidence columns (peak values, counts, the terms behind a flag) are carried
through on this frame. \code{Transform_Rate()} summarises them away, but the
unsummarised \code{Analysis_Input} is saved beside every other step's output, so
the numbers behind a flag stay one file away from the flag itself.
}
\keyword{internal}
