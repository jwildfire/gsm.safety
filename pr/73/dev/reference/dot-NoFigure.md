# The empty `analyticsInput` frame

What a census metric publishes when it has measured nothing: no row at
all, rather than a row reading zero.

## Usage

``` r
.NoFigure()
```

## Value

`data.frame` with the `analyticsInput` columns and no rows.
