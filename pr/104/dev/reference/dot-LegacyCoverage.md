# The `Coverage` table

Its columns, and no rows, until the coverage metric lands
([\#58](https://github.com/jwildfire/gsm.safety/issues/58)). Counting it
here from a raw domain is what this rebuild removed; an empty table is
what the reader is told, and its reader already says so in words.

## Usage

``` r
.LegacyCoverage()
```

## Value

`data.frame` with the coverage columns and no rows.
