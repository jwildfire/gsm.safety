% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Derive_ExtremeValueFlag.R
\name{Derive_ExtremeValueFlag}
\alias{Derive_ExtremeValueFlag}
\title{Flag results beyond the FDA extreme-value thresholds}
\usage{
Derive_ExtremeValueFlag(
  dfResults,
  dfThresholds = gsm.safety::FDA_ExtremeValues,
  strTestCol = "TEST",
  strValueCol = "STRESN",
  strUnitCol = "STRESU",
  lParameterValues = DefaultExtremeParameters(),
  strOutCol = "ExtremeValueFlag"
)
}
\arguments{
\item{dfResults}{\code{data.frame} Long-format results, one row per participant
per test per visit, in the shape of \code{ExampleData("adbds")}.}

\item{dfThresholds}{\code{data.frame} The thresholds, in the shape of
\link{FDA_ExtremeValues} (at least \code{Parameter}, \code{UnitSystem}, \code{Unit}, \code{Low},
\code{High}). Default: \code{FDA_ExtremeValues}.}

\item{strTestCol}{\code{character} Column holding the test name. Default: \code{"TEST"}.}

\item{strValueCol}{\code{character} Numeric result column. Default: \code{"STRESN"}.}

\item{strUnitCol}{\code{character} Unit column. Default: \code{"STRESU"}.}

\item{lParameterValues}{Named \code{list} mapping each threshold \code{Parameter} to
the test name(s) the data use for it. Default: the names
\code{ExampleData("adbds")} uses.}

\item{strOutCol}{\code{character} Name of the flag column. Default:
\code{"ExtremeValueFlag"}; the direction is added as \verb{<strOutCol>} with
\code{Flag} replaced by \code{Direction}. Neither may already be a column of
\code{dfResults}: the function appends, it never overwrites.}
}
\value{
\code{dfResults} with two columns appended: the flag (\code{logical}, \code{TRUE}
beyond a threshold, \code{FALSE} within, \code{NA} where the parameter or unit could
not be matched or the result is missing) and the direction (\code{"low"},
\code{"high"} or \code{NA}).
}
\description{
Adds the flag the FDA Standard Safety Tables and Figures Integrated Guide
v2.0 removes records on: a result below the low or above the high threshold
of Appendix Tables 58 (chemistry), 59 (hematology) or 60 (vital signs) is
treated as a likely laboratory or recording error and excluded from the
mean change from baseline over time figures (sections 2.4, 2.4.2, 2.5.1).
Requirement \code{FDA-RULE-004}; the unit matching is \code{FDA-RULE-005}.
}
\details{
The guide prints every threshold in US-conventional and SI units, and
\link{FDA_ExtremeValues} carries both. A record is compared against the row for
the unit it carries: a result in \code{mg/dL} against the US thresholds, in
\code{mmol/L} against the SI ones. A record whose unit matches neither is left
\code{NA} and named in a message rather than compared on the wrong scale;
spellings of the same unit are folded (and mEq/L and mmol/L treated as one
unit for the monovalent electrolytes only) but nothing is converted. A missing
threshold in one direction (an \code{N/A} in the guide) is no bound in that
direction, and the threshold itself is not extreme.

The flag is added, never applied: nothing is dropped or aggregated, and the
renderer that draws a mean-change figure filters on it (the design's L1
contract).
}
\examples{
dfLabs <- ExampleData("adbds")
dfLabs <- Derive_ExtremeValueFlag(dfLabs)
table(matched = !is.na(dfLabs$ExtremeValueFlag), extreme = dfLabs$ExtremeValueFlag, useNA = "ifany")

}
\seealso{
\link{FDA_ExtremeValues} for the thresholds and their provenance.

Other FDA derivations:
\code{\link[=Derive_AbnormalityLevel]{Derive_AbnormalityLevel()}},
\code{\link[=Derive_ULNMultiple]{Derive_ULNMultiple()}}
}
\concept{FDA derivations}
