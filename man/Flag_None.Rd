% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Flag_None.R
\name{Flag_None}
\alias{Flag_None}
\title{Publish a metric's rows without a flag}
\usage{
Flag_None(dfAnalyzed)
}
\arguments{
\item{dfAnalyzed}{\code{data.frame} Output of an \verb{Analyze_*} step, carrying at
least \code{Score}.}
}
\value{
\code{dfAnalyzed} with an integer \code{Flag} column of \code{NA}.
}
\description{
The step a \strong{descriptive} metric calls where a flagging metric calls
\code{gsm.core::Flag()}. It adds the \code{Flag} column \code{gsm.core::Summarize()}
requires and leaves it empty.
}
\details{
Empty, not zero. \code{0} is a flag value that means \emph{measured and found fine} —
a green light. \code{NA} is the ecosystem's existing way of saying this number
does not flag: gsm.kri's \code{srs0001} site risk score has published its rows
that way in production throughout, declaring no threshold and calling no
flagging step. Because a metric with no threshold contributes no weights,
\code{gsm.kri}'s risk-score builder skips it automatically, so a descriptive
metric cannot move a site's risk score.
}
\examples{
dfAnalyzed <- data.frame(
  GroupID = "AA-AA-000-0000", GroupLevel = "Study",
  Numerator = 12, Denominator = 760, Metric = 12 / 760, Score = 12
)
Flag_None(dfAnalyzed)

}
