# The `Disposition` table

The `Disposition` table

## Usage

``` r
.LegacyDisposition(dfFigures)
```

## Arguments

- dfFigures:

  `data.frame` From
  [`Report_CensusFigures()`](https://jwildfire.github.io/gsm.safety/dev/reference/Report_CensusFigures.md).

## Value

`data.frame` with `State` and `Participants`, largest first.
